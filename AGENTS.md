Always use snake case for database table names and column names.
Always use bigint for id database columns.
Use created_at, updated_at, and deleted_at for all database tables and use timestamptz.
Never use abbreviations in variable names.
